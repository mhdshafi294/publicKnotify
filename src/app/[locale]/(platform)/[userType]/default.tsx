/**
 * A placeholder component that renders nothing.
 *
 * This component can be used as a default or fallback in scenarios where a component
 * is required but no specific implementation is needed.
 *
 * @returns {null} This component does not render any content.
 */
const Default = () => {
  return null;
};

export default Default;
